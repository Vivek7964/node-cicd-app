apiVersion: v1
kind: Service

metadata:
  name: node-cicd-app
  namespace: node-app

spec:
  type: NodePort
  selector:
    app: node-cicd-app

  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
